//Soal Nomer 1

/* var batasBawah = 2;
var batasAtas = 20; */

/*console.log('LOOPING PERTAMA');

while (batasBawah <= batasAtas) 
    {console.log(`${batasBawah} - I love coding`);
    batasBawah += 2;}*/

/*console.log('LOOPING KEDUA');

while (batasBawah <= batasAtas) 
    {console.log(`${batasAtas} - I will become fullstack developer`);
    batasAtas -= 2;} */

// Soal Nomer 2
 
/*console.log('LOOPING PERTAMA')

for(var batasBawah = 1; batasBawah<=batasAtas; batasBawah ++)
    {console.log(`${batasBawah} - I love coding`)}*/

/*console.log('LOOPING KEDUA')

for(var batasAtas; 1<=batasAtas; batasAtas -- )
    {console.log(`${batasAtas} - I will become fullstack developer`)}*/

// Soal Nomer 3

/*for (var counter = 1 ; counter <= 100 ; counter ++)
    {var check = counter%2;
        if (check === 0) {console.log(`${counter} adalah GENAP`);}
        else {console.log(`${counter} adalah GANJIL`);}
    } */

/*for (var counter = 1 ; counter <= 100 ; counter +=2)
    {var check = counter%3;
        if (check === 0) {console.log(`${counter} Kelipatan 3`);}
        else {console.log(`${counter}`);}
    } */

/*for (var counter = 1 ; counter <= 100 ; counter +=5)
    {var check = counter%6;
        if (check === 0) {console.log(`${counter} Kelipatan 6`);}
        else {console.log(`${counter}`);}
    } */

/*for (var counter = 1 ; counter <= 100 ; counter +=9)
    {var check = counter%10;
        if (check === 0) {console.log(`${counter} Kelipatan 10`);}
        else {console.log(`${counter}`);}
    } */